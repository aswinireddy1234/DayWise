for logs in `ls *.log`
do
folderName=`echo $logs | awk -F. '{print $1}'`;
echo $logs;
echo $folderName;
if [ -d $folderName ];
then
mkdir $folderName;
cp $logs $folderName;
$logs>>$(date +"$2_$3_$6");
done
